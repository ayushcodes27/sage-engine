## Dashboard Layout

- `bridge.js`: Kafka consumer that forwards telemetry to Socket.IO clients.
- `sage-ui/`: Vite/React dashboard frontend.

The static dashboard served from `public/` has been removed. Run the bridge from this directory and the UI from `sage-ui/`.
